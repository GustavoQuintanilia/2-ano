 <footer style="background-color: #F08080;">

    <nav class="navbar" >
      <div class="container-fluid">
        <p class="navbar-brand">
            {{#each dados}}
                <h3>{{this.nome}}</h3>
            {{/each}}
        </p>
        <p class="navbar-brand">
            {{#each dados}}
                <h3>Data de publicação: {{this.data}}</h3>
            {{/each}}
        </p>
      </div>
    </nav>
  </footer>